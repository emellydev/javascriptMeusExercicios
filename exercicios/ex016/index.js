// Manipulando Arrays

// índice:     0       1        2
/* let nomes = ['ana', 'pedro', 'joão'];

nomes[3] = 'Maria' // O índice 3 não está definido, então ele será adicionado
console.log(nomes) 
// Saída: [ 'ana', 'pedro', 'joão', 'Maria' ] */

const array = [1, 2, 3, 4, 5];
array.pop();       // Remove o último elemento.
array[0] = 1024;   // Altera o primeiro elemento.
console.log(array); // [1024, 2, 3, 4]

const array = [1, 2, 3, 4, 5];
array = 'mensagem'; // Erro: Atribuição a uma variável constante.



/* 
nomes[2] = 'Eduardo'

['Luiz, 'Maria', 'Eduardo']

aluno[3] = João // adiciona joão

// numeros, boolean e strings 

*/

