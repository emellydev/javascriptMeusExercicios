/* function soma(x, y) {
    return `A soma de ${x} e ${y} é: ${x + y}`
}

const resultado = soma(10, 5);
console.log(resultado)
// Saída: A soma de 10 e 5 é: 15
*/

/* function soma(x, y) {
    console.log(`A soma de ${x} e ${y} é ${x + y}`);
}
soma(20, 5)
// Saída: A soma de 20 e 5 é 25
*/

/*
const raiz = n => {
	return n ** 0.5;
	
};

console.log(raiz(9));
// Saída: 3
*/

/*
const arrowFunction = (x, y) => x + y;
console.log(arrowFunction(3, 1));  // Saída: 4
*/

function soma( x =1,  y = 1) {
    return(x + y)
}
resultado = soma()
console.log(resultado)
// Saída: 2
