// Declaração de variáveis
const nome = "Maria";
const idade = 28;
const cidade = "São Paulo";

// Exemplo de template literals 
const mensagem = `Meu nome é ${nome}, tenho ${idade} anos e moro em ${cidade}.`;

console.log(mensagem);

// Exemplo de string multilinha com template literals
const textoMultilinha = `
Linha 1: Esta é uma string
Linha 2: com várias linhas
Linha 3: usando template literals.
`;

// Exibindo a string multilinha no console
console.log(textoMultilinha);
